name: say Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: say hello step 
        run: |
          echo "Hello World!"
          echo "my first github action"
      - name: Now it is 
        run:  date
      - name: Directory contnet
        run: ls -l